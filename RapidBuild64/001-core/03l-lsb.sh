#!/bin/bash

source ../spec.sh
source ./module_spec.sh

# What to do about this?
# ln -sf /${LIBDIR}/ld-linux.so.2 ${DIR}/${LIBDIR}/ld-lsb.so.3
# ln -sf /${LIBDIR}/ld-linux*.so.2 ${DIR}/${LIBDIR}/ld-lsb.so.3
